# Nodes registry (non-binding)

Purpose: list independent nodes associated with the AIC.

How to register: submit PR with `nodes/<node-name>.md` following the template in `JOIN_GUIDE.md`.

Note: Registry is just a directory; no endorsement is implied.
